{
  "name": "mcp-nano-banana",
  "version": "0.1.0",
  "description": "MCP server for Nano Banana image generation (Google Gemini image models). Authenticates with the user's nb_live_ API key (credits bought on the site), then generates images, analyses images into structured JSON, and writes Instagram captions through the site's metered backend. Users never handle a Google key.",
  "type": "module",
  "bin": {
    "mcp-nano-banana": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
